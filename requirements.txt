# Flask Web Framework
Flask==3.0.0

# Jinja2 Template Engine (included with Flask but listed for clarity)
Jinja2==3.1.2

# Werkzeug WSGI Web Application Library (included with Flask)
Werkzeug==3.0.1

# Click Command Line Interface (included with Flask)
click==8.1.7

# MarkupSafe (included with Flask)
MarkupSafe==2.1.3

# Blinker (included with Flask)
blinker==1.7.0

# ItsDangerous (included with Flask)
itsdangerous==2.1.2

# Development and Production Dependencies (Optional)

# For production deployment
gunicorn==21.2.0

# For environment variable management
python-dotenv==1.0.0

# For database support (if needed in future)
SQLAlchemy==2.0.23
Flask-SQLAlchemy==3.1.1

# For form handling (if needed in future)
Flask-WTF==1.2.1
WTForms==3.1.1

# For user authentication (if needed in future)
Flask-Login==0.6.3

# For email support (if needed in future)
Flask-Mail==0.9.1

# For file uploads (if needed in future)
Flask-Uploads==0.2.1

# For CORS support (if needed in future)
Flask-CORS==4.0.0

# For API development (if needed in future)
Flask-RESTful==0.3.10

# For caching (if needed in future)
Flask-Caching==2.1.0

# For security headers (if needed in future)
Flask-Talisman==1.1.0

# For rate limiting (if needed in future)
Flask-Limiter==3.5.0 